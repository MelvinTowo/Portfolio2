'use client'

import Image from 'next/image';
import { useState } from 'react';
import {motion} from 'framer-motion';

const projectMessage = `Welcome to my Projects and publications page. Here I share ideas and works that I've done`
const projects = []


export default function ProjectPage() {
  return (
    <>
      <section className='min-h-screen flex items-center justify-center bg-gradient-to-br from-black-900 via-gray-900 to black-900 px-6'>
        {/* Projects section with content and expandable list */}
        
      </section>

      <section>
        {/* Publications and emerging technologies section */}
      </section>

      <section>
        {/* Checkout VOLT section */}
      </section>
    
    </>
  );
}